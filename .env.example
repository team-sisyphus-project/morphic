# Autosquad preview: variables the platform should know about.
# DATABASE_URL is injected by the platform (managed PostgreSQL) and must not be listed here.

# Model and search keys. Set real keys to get answers; these defaults let the app boot without them.
OPENAI_API_KEY=preview-no-key
TAVILY_API_KEY=preview-no-key

# OPTIONAL defaults so the app boots as-is
DATABASE_SSL_DISABLED=true
ENABLE_AUTH=false
ANONYMOUS_USER_ID=anonymous-user
SEARCH_API=tavily
